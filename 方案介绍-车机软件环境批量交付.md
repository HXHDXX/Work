# 车机软件环境批量交付 —— 方案介绍

> 版本：v1.0（2026-09-08）
> 范围：麒麟 V10 车机（飞腾 arm64，离线内网）与 Android 车机（RK/高通平台）的软件环境批量部署
> 本文是**总览**；各专项细节见配套文档（见 §3 资产清单）

---

## 1. 背景与问题

| 现状 | 痛点 |
|---|---|
| 车机为离线内网设备，无互联网 | 无法在线装软件，一切介质需中转 |
| 麒麟侧含自编译组件（Qt 5.14.2 全套、fcitx 插件、达梦 QDM 驱动）与商业软件（DM8） | 首次部署成本极高（编译约 4 小时 + 8 个坑位）；现场复刻不可靠 |
| 同型号设备需要批量交付 | 逐台手工安装不现实 |

## 2. 方案总览：两条交付路线，按场景组合

```
路线A：整机镜像（同型号批量，最快）
   母版机调好 → 规范化封版 → 导出镜像 → 批量写入 → 首启/写号去重

路线B：组件部署包（异构/已装系统/增量更新）
   达梦+QDM 部署包 | Qt 环境部署包 → 一键脚本安装 → 自动验证
```

| 场景 | 推荐路线 |
|---|---|
| 新出厂同型号车机，整批初始化 | **A 整机镜像**（麒麟 20~40 分钟/台；Android 3~10 分钟/台） |
| 已在用设备补装/升级个别组件 | **B 部署包**（幂等脚本，不动系统其他部分） |
| 混合fleet | A 打底 + B 补丁（大版本镜像、小变更部署包） |

**核心思想：一切安装/编译/配置只在母版或首次完成，交付物永远是"镜像或包 + 幂等脚本 + 文档"，绝不重复过程。**

## 3. 交付资产清单（三大包，随文档移交）

| 包 | 位置 | 内容 | 状态 |
|---|---|---|---|
| **达梦+QDM 部署包** | `dm8-deploy/`（配合 dm8 介质目录） | `install_dm8_qt.sh`（一键：ISO校验→静默装DM8→建实例→服务→disql验证→ldconfig→编QDM驱动→Qt端到端冒烟）+ `INSTALL.md`（全流程实录+8坑位表） | ✅ 已在真机实测（RC=0） |
| **Qt 环境部署包** | `qt-env-deploy/`（266M） | `qt_backup.tar.gz`（174M：/opt/Qt5.14.2 全套含 QDM/fcitx 双插件 + 10模块源码树 + 配置）+ `debs/`（89个离线deb：QtCreator 4.11.0/依赖/gdb/cmake）+ `install_qt_env.sh`（幂等恢复）+ `README-QT-ENV.md` | ✅ 已在真机实测（RC=0，免编译恢复 4 小时工作量） |
| **整机镜像工具与方案** | `deploy-playbooks/` | `imagetool/`（母盘封版 `prep_for_image.sh` + 首启去重 `firstboot-unique`）+ 《整机镜像批量交付方案-麒麟V10与Android车机.md》 | 麒麟侧脚本已装源机 `/root/imagetool/`；镜像制作需现场 U 盘操作 |

## 4. 麒麟 V10 车机：交付流程摘要

**路线 A（整机镜像）** —— 详见《整机镜像批量交付方案》第 3 章

> ⚠️ **2026-09-24 修订**：SSH 热镜像+fsfreeze 路线已因母版机事故废弃，改为零修改路线（A1 拆盘直读首选 / A2 Live U 盘冷读 / A3 纯读热拷贝），详见《整机镜像方案修订-教训与零修改路线-20260924.md》
1. 母版机（如 192.168.1.222）软件就绪 → `sudo bash /root/imagetool/prep_for_image.sh && shutdown`
2. arm64 Live U 盘启动 → `dd if=/dev/sda | gzip > golden-vN.img.gz`（98G 盘→约 10G 镜像）
3. 目标机逐台 `gunzip -c … | dd of=/dev/sda`（>10 台建议 PXE）
4. 首启自动去重（machine-id/SSH密钥/主机名），仅剩麒麟激活与密码逐台处理

**路线 B（部署包）**
- 装 DM8+QDM：`sudo bash install_dm8_qt.sh <dm8介质目录>`
- 装 Qt 环境：`sudo bash install_qt_env.sh <qt-env-deploy目录>`
- 两脚本幂等可重跑，内置全部坑位规避与端到端验证

**环境规格**（当前母版实际内容）：Qt 5.14.2（10 模块，含 Charts/QDM/fcitx）、QtCreator 4.11.0、gdb 9.2、DM8（DAMENG@5236，License 至 2027-06-25）、systemd 服务自启。

## 5. Android 车机：交付流程摘要（详见同文档第 4 章）

1. 定 SoC 通道：**RK → RKDevTool/update.img；高通 → QFIL(9008)；通用 → fastboot**
2. 母版机调版 + 处理 AVB（`vbmeta --disable-verity --disable-verification` 或平台重签）
3. 镜像来源：固件构建产物（首选）或母机分区读回（**persist/metadata 校准分区严禁克隆**）
4. 批量：RK 量产工具一拖多烧录架 / QFIL 逐工位 / fastboot 循环
5. 产线写号：serialno、WiFi/BT MAC、密钥、屏幕/摄像头标定
6. 升级走 OTA 差分或 recovery 整包，与麒麟侧"部署包补丁"理念一致

## 6. 已验证与待办

| 项 | 状态 |
|---|---|
| dm8 部署包（幂等跳过 + QDM 重编 + 冒烟路径） | ✅ 真机实测 |
| qt-env 部署包（幂等恢复 + 验证 + 达梦联动） | ✅ 真机实测 |
| DM8 全新安装路径 | 逻辑固化自手工实录，未在真机重演（不擦库无法测） |
| Qt 全新解包路径 | 原语（tar/dpkg）已验证，组合流程未在空机演练 |
| 整机镜像（麒麟） | 脚本就绪，需现场 U 盘实操 |
| 整机镜像（Android） | 方案就绪，需确认 SoC 平台与解锁状态后实施 |
| 建议：内网 apt 镜像、自产组件 deb 化 | 待立项（长期最优） |

## 7. 使用索引

```
想干什么 → 看哪份
─────────────────────────────
批量克隆整机         → 整机镜像批量交付方案-麒麟V10与Android车机.md
装/迁移达梦+Qt驱动   → dm8-deploy/INSTALL.md
新机恢复Qt开发环境   → qt-env-deploy/README-QT-ENV.md
了解全部坑位(麒麟)   → dm8-deploy/INSTALL.md 第五节
对外介绍本方案       → 本文
```
