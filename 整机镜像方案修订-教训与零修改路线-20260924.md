# 整机镜像方案修订 —— 事故教训与零修改路线

> 版本：v1.0（2026-09-24）
> 性质：《整机镜像批量交付方案》麒麟路线 A 的修订与替代，因 2026-09-24 母版机镜像事故而立
> 本文取代原路线 A 中"SSH 热镜像 + 服务静默 + fsfreeze"的做法；该做法已列入禁用清单（§2 L4）

---

## 0. 事故回顾（一段话）

按原方案对母版机（kylin@192.168.1.222，飞腾 D3000M / Kylin V10 SP1 / MasonSemi MI600 1TB）做 SSH 热镜像：封版（prep_for_image.sh，RC=0）→ 停 lightdm/DM8/journald + swapoff + /boot /boot/efi /backup 只读重挂 → **fsfreeze 冻结 / 与 /data**（07:58:11 成功）。自动解冻看门狗因**缺 chmod 静默死亡**→ 冻结 5 小时无人解除 → SSH 全阻塞（PAM 会话建立要写被冻盘上的 wtmp/lastlog，连 SessionType=none 认证也挂；探测把 sshd 积压塞满 Exceeded MaxStartups）→ 被迫物理断电 → **设备卡 logo 无法启动**：ESC 进控制台确诊 `mount: /boot/efi: wrong fs type, bad superblock on /dev/sda1` —— EFI 分区（vfat，无日志）超级块在多次硬断电中被固件写入打断而损坏；根分区 sda3 本身挂载正常无报错。initramfs 无提示符，机器上不可自救，待 arm64 Live U 盘 `fsck.vfat -y /dev/sda1` 修复。

## 1. 教训（按因果链编号）

| # | 教训 | 本次对应 |
|---|---|---|
| L1 | **解冻路径独立性**：任何冻结/停机操作前，枚举全部恢复路径，每条必须独立于被冻结的文件系统；看门狗装完必须 `chmod +x` 并 `pgrep` 验证存活，stderr 不得吞掉 | 看门狗 644 不可执行 + `>/dev/null 2>&1` 静默死亡 = 直接根因 |
| L2 | **远程冻结根分区 = 自断退路**：sshd 会话建立（PAM/wtmp/lastlog）依赖被冻盘上的文件，冻结后新会话全部阻塞，远程完全失能，只剩物理接触 | 冻结后所有 ssh 挂死，连 SessionType=none ControlMaster 也失败 |
| L3 | **无日志文件系统是最弱一环**：ext4 有 journal，冻结态断电确实干净（sda3 挂载无报错证实）；但 ESP 是 vfat 无 journal，卡死后每次硬断电都可能打断固件写 ESP → 超级块损坏。链路强度 = 最弱一环 | sda1 vfat 损坏导致整机卡 logo；sda3 反而无恙 |
| L4 | **原方案的 Live U 盘路线是对的**：SSH 热捷径省了一个 U 盘，引入了全部风险。远程操作省物理接触的收益 << 远程失能的风险 | 若按原文档走 U 盘冷启动 dd，本事故不可能发生 |
| L5 | **冻结期间的探测行为有害且致盲**：每次 ssh 探测留一个 D 态会话塞满 sshd MaxStartups；`2>/dev/null` 丢弃 stderr 使"已断电"与"冻结可达"两种状态不可分辨，误判 2 小时 | banner 在 stderr；改用 ping+TCP banner 探测后才分辨出机器一直活着 |
| L6 | **母版机重启会消耗封版标记**：firstboot-unique 在母版自身重启时运行，吃掉 `/etc/.needs-uniq` 并自禁用；母版任何重启后必须重跑 `prep_for_image.sh` 再成像，否则克隆机首启去重失效 | 本次断电重启后标记已被消耗 |
| L7 | **兜底资产预先核实**：路线 B 部署包（dm8-deploy / qt-env-deploy）是最终安全网，交付文档应记录其存放位置并定期核实可得 | 用户确认手头有；工作站本地没有 |
| L8 | **理论安全 ≠ 现实安全**："fsfreeze=同步落盘，断电=干净卸载"在纸面成立，但没算 vfat ESP、固件行为、多次断电的叠加。不可把系统逼进"只有理论安全"的路径 | 冻结 5h 后被迫断电即进入该状态 |

## 2. 修订后的路线 A：零修改镜像

**定义"不修改设备"**：不停服务、不 remount、不冻结、不写盘——设备保持原样，磁盘只被读取。三条子路线按优先级：

### A1（首选）拆盘直读 —— 零设备参与，物理层只读

```
断电 → 拔 SSD（MasonSemi MI600 1TB，机上识别为 sda，注意确认 SATA/NVMe 接口）
→ USB 转接盒接工作站
→ 工作站本地：dd if=/dev/sdX bs=8M status=progress | pigz -1 -p8 > golden-vN.img.gz
```

- **优点**：无网络瓶颈（本地读+本地 8 线程压缩，全盘 953.9G 预计 40~90 分钟）；设备软件零参与、零一致性风险（断电拔盘时 initramfs 阶段 / 只读挂载，盘面静止）
- **缺点**：物理拆装；母版停机窗口=成像时长
- **前置**：USB-SATA（或 NVMe）转接盒一只；插上后 `lsblk` 确认设备名再 dd（**写错盘=灾难，双向都要人工核对**）

### A2（不拆盘）Live U 盘冷读 —— 原方案本意，设备盘只读

```
arm64 Live U 盘启动（固件启动菜单选 U 盘，不写设备盘）
→ Live 环境：设备盘保持不挂载（或 -o ro）
→ dd if=/dev/sda bs=8M | pigz -1 -p6 | ssh 工作站 'cat > golden-vN.img.gz'
```

- **优点**：不动设备软件；冷盘读取一致性好；顺便就是修复本次 ESP 的同一支 U 盘
- **缺点**：需 arm64 Live U 盘；传输受千兆网限制（压缩后约 20~60G，10~30 分钟，瓶颈在读+压 1~2 小时）
- **注意**：Kylin V10 SP1 arm64 安装 ISO 即可（含 fsck.vfat / grub 工具）；Live 里 Rescue 模式最方便

### A3（完全不接触设备）纯读热拷贝 —— 接受 journal 自愈的一致性折扣

```
ssh kylin@192.168.1.222 'sync; dd if=/dev/sda bs=8M iflag=fullblock status=none | pigz -1 -p6' > golden-vN.img.gz
```

- **零停机零修改**：不 sync 以外任何操作；纯读
- **一致性代价**：镜像中的 ext4 = "如同运行中突然断电"状态 → 目标机首挂靠 journal 重放自愈（绝大多数情况无损恢复）；ESP 为 vfat，但运行中固件几乎不写 ESP，风险远小于断电场景
- **加固**：成像窗口选设备空闲时段（无编译/大写入）；克隆机首启加 `e2fsck -f` 验证一次
- **适用**：母版不能停机时的取舍

### 禁用清单（本次事故后）

- ✗ 远程 `fsfreeze` 生产机根分区（L1/L2）——除非解冻路径已独立验证且不依赖被冻盘
- ✗ 为成像而停服务/ remount 的"SSH 静默"方案（L4）——一律改 A1/A2
- ✗ 无执行位/未验证存活的看门狗（L1）
- ✗ 冻结/异常状态下 `2>/dev/null` 的盲目 ssh 探测（L5）——改 ping + TCP banner 探测

## 3. 成像前封版（保留但流程加固）

母版**每次重启后**（L6）成像前重跑：`cd /root/imagetool && bash prep_for_image.sh`（幂等；恢复 `/etc/.needs-uniq` 标记 + 启用 firstboot-unique）。A1 拆盘场景：装回盘开机跑一次 prep 再拆盘，或拆盘前跑。

## 4. 镜像验收清单（不变，三条路线通用）

```bash
pigz -dc golden-vN.img.gz | wc -c        # == 源盘 blockdev --getsize64（本机=1024209543168）
pigz -dc golden-vN.img.gz | head -c 2M > pt.bin && fdisk -l pt.bin   # GPT 分区表完好
sha256sum golden-vN.img.gz               # 归档校验值
# 首台克隆机：首启后核对 machine-id/SSH主机密钥/hostname 已变化（firstboot-unique 生效）
```

## 5. 本次设备恢复（待执行，Live U 盘就绪后）

1. arm64 Live U 盘启动 → `fsck.vfat -y /dev/sda1`（大概率一步修复，GRUB 文件应仍在：固件能读到 grub 说明 FAT 数据区尚可）
2. 顺手 `fsck -y /dev/sda2 /dev/sda3 /dev/sda5`；`mount /dev/sda1 /mnt && ls -R /mnt/EFI` 验证 GRUB
3. GRUB 缺失则 chroot 重装：`grub-install --efi-directory=/boot/efi --no-nvram && grub-mkconfig -o /boot/grub/grub.cfg`
4. 重启验证 → 健康 → 重跑 prep 封版 → **按 A1 或 A2 重新成像**

---

## 6. 工具化落地（2026-09-24 14:13）

三条路线与写回已全部脚本化：`/Workspace/Work/imagetool-v2/`
- `a1-usb-disk-image.sh` —— A1 拆盘直读（修ESP+恢复封版+镜像+校验+manifest 一条龙）
- `a2-live-usb-image.sh` —— A2 Live U 盘冷读（ssh 推送/本地落盘双模式）
- `a3-hot-read-image.sh` —— A3 纯读热拷贝（零停机）
- `restore-image.sh` —— 写回/克隆（可移动盘保护）

全部通过 `bash -n` 语法自检（脚本首行亦内置自检——教训 L1 固化）；写盘级操作前置三重核验（总线/型号/容量 + 挂载检查）。
