# AGENTS.md 优化设计

> 日期: 2026-07-02
> 参考: `/home/guangbin/Documents/HXProjectTemplate/AGENTS.md`
> 目标: 将本仓库 AGENTS.md 从「C++ 代码项目模板」改写为「文档/知识工作区」定制版

## 背景

本仓库（`Work`）是文档/知识工作区（工作计划、技术方案、环境配置笔记、Obsidian），实际 C++ 开发发生在其他仓库。当前 AGENTS.md 从 HXProjectTemplate 复制而来，其中多节针对代码项目，对本工作区不适用。

## 改动文件

- `AGENTS.md`（清单层）
- `docs/agents/core/principles.md`（详述层）

## 第 1 层：AGENTS.md 清单

### 保留不动

- AGENTS.md 读写元规则
- 上下文与沙盒策略 (Context-Mode)
- 知识查询与沉淀

### 改写

- 核心原则 #1：`中英双语思维链` → `英语思维，中文交互`

### 新增（行为准则，追加在 Trivial 豁免之后）

- 系统调试：禁止猜测式修复，必须先复现问题、读完整错误与堆栈，一次只改一处并验证；连续 3 次尝试失败必须回滚至已知良好状态，并升级提示用户介入
- 根因优先：禁止用判空或 try-catch 掩盖非预期值，必须追溯其产生根因；症状式修补不视为修复
- 沟通纪律：交付时必须说明变动内容、演进逻辑、潜在顾虑与未覆盖项，禁止仅抛出孤立代码块；禁止使用"应该可以/大概能行"等模糊表述，不确定性必须量化为明确的待验证点
- 反模式自检：命中以下任一模式必须立即停止而非硬推——大扫除（借机重构无关区域）、过早抽象（未出现两次重复即抽象）、乐观路径（只处理成功流忽略异常分支）、失控重构（修复跨文件级联扩散）

### 删除（不适用于文档工作区）

- 项目目录结构（引用 libs/、cross-library-examples/）
- 依赖库使用优先级（C++17 / libs/）
- Git Worktree 线性历史铁律（代码隔离开发）
- 编译与验证机制（container/build.sh）

## 第 2 层：docs/agents/core/principles.md 详述

重构为 14 条编号原则。核心原则（#1-4）要点说明；行为准则（#5-14）统一采用 **红线 / 自检 / 反例** 三段式。

### 核心原则

| # | 原则 | 处理 |
|---|---|---|
| 1 | 英语思维，中文交互 | 措辞改写：用英文逐步思考，用中文回答 |
| 2 | 先理解再行动 | 保留 |
| 3 | 交付结果必须有验证方案 | 保留，反例改为文档场景 |
| 4 | 遇到问题先检索 ./docs | 保留 |

> 删除原模板 #3「通过构建系统理解」（CMakeLists.txt，无构建系统；要义已被 #2 覆盖）

### 行为准则（红线/自检/反例三段式）

| # | 原则 | 处理 |
|---|---|---|
| 5 | 澄清意图再动手 | 已有→升级 |
| 6 | 深度调研 | 已有→升级 |
| 7 | 简单优先 | 已有→升级，删除「C++ 常见过度工程」专节，反例通用化 |
| 8 | 精准修改 | 已有→升级 |
| 9 | 验证先行 | 已有→升级，验收标准改为文档可检查项 |
| 10 | Trivial Task 豁免 | 已有→升级，"构建验证"改为"链接/引用验证" |
| 11 | 系统调试 | 🆕 新增，去掉 gdb 引用，保留流程纪律 |
| 12 | 根因优先 | 🆕 新增 |
| 13 | 沟通纪律 | 🆕 新增 |
| 14 | 反模式自检 | 🆕 新增，标注与 §7/§8 互为表里 |

## 不动的文件

- `docs/agents/index.md`
- `docs/agents/writing-guide.md`
- `docs/agents/core/context-strategy.md`
- `docs/agents/core/structure.md`

## 验收标准

1. `AGENTS.md` 不再包含 container/build.sh、C++17、libs/、cross-library-examples/、Worktree 相关字样
2. `AGENTS.md` 行为准则包含 10 条（原 6 + 新 4）
3. `principles.md` 行为准则部分每条均含「红线 / 自检 / 反例」三要素
4. `principles.md` 不含 CMakeLists.txt、gdb、C++ 过度工程等代码专属内容
5. 两文件无残留指向已删除章节的悬空引用
6. `docs/agents/index.md` 中「目录结构」描述仍与新文件结构一致（structure.md 引用路径有效）
